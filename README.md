# ruben.dev

Personal portfolio and notes site — Vue 3 + Vite, deployed to GitHub Pages.

## Dev

```bash
npm install
npm run dev
```

## Deploy

Pushes to `main` automatically trigger the GitHub Actions workflow which builds and deploys to `rubendebeer.github.io/Rubi-Tubes/`.
